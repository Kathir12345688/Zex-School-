# React + Vite

This frontend is configured to use the Django backend through Vite environment variables.

## Environment configuration

Create a local environment file named `.env` in the frontend directory with:

```
VITE_API_URL=https://zex-school.onrender.com
```

For Vercel deployments, set the same variable in the project environment settings.
